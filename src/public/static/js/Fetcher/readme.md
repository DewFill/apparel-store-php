Fetch.init(request, url, data)
.setRequest()
.setUrl()
.setData([])
.log(bool)
.fetch()
    new Promise(respond => new ResultParser(respond))
    .then(result_parser => result_parser.parse())
.fetchSync()
    return new ResultParser(respond).parse()